@typeparam T
@if (ChildContent != null)
{
    foreach (T item in Items)
    {
        <tr class="@RowClass @(SetSelected(item))" @onclick="() => SelectTheItem(item)" @ondblclick="() => DoubleClick(item)">
            @ChildContent(item)
        </tr>
    }

}