@typeparam T
@if (ChildContent != null)
{
    foreach (T item in Items)
    {
        <tr class="@RowClass @(SetSelected(item))" @onclick="() => SelectedItem = item">
            @ChildContent(item)
        </tr>
    }

}